# OpenJarvis configuration - safe to share, no secrets here
# Copy to ~/.openjarvis/config.toml and set OPENROUTER_API_KEY as an env var

[engine]
default = "cloud"

[intelligence]
default_model = "openrouter/x-ai/grok-4.1-fast"
preferred_engine = "cloud"
